/**
 * Empty placeholder for the dashboard route. Renders nothing visible — later
 * slices (#11) replace this with real overview widgets.
 */
export default function DashboardPlaceholder() {
  return (
    <div data-testid="dashboard-placeholder" className="text-sm text-slate-500">
      Dashboard — coming soon.
    </div>
  );
}
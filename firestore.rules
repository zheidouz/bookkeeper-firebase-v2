rules_version = '2';

// Locked-down placeholder. All reads/writes denied by default.
// Later slices (auth, data model, rules hardening) relax these rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
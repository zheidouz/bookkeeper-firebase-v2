rules_version = '2';

// Locked-down placeholder. Storage is deny-all until later slices open it.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}